import React from "react";
import {View, Text, StyleSheet} from 'react-native';

const ProfileDigs = (props) => {
    return (
        <View>
            <Text>ProfileDigs</Text>
        </View>
    )
};

const styles = StyleSheet.create({

});

export default ProfileDigs;